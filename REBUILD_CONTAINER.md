# 🔄 Пересборка Dev Container

## Зачем нужно

Dev контейнер обновлен и теперь включает:
- ✅ Node.js + npm + wscat
- ✅ Python 3.11 + pip + websockets
- ✅ Инструменты для работы с WebSocket

## Как пересобрать

### Шаг 1: Откройте Command Palette

**Windows/Linux**: `Ctrl + Shift + P`
**Mac**: `Cmd + Shift + P`

### Шаг 2: Введите команду

Начните печатать:
```
rebuild
```

Выберите из списка:
```
Dev Containers: Rebuild Container
```

### Шаг 3: Подтвердите

Нажмите `Enter` или кликните на команду.

### Шаг 4: Дождитесь завершения

Процесс займет **3-5 минут**. Вы увидите:
```
[Building] Building image...
[Building] Installing features...
[Building] Running postCreateCommand...
```

### Шаг 5: Проверьте установку

После пересборки откройте терминал и проверьте:

```bash
# Проверка Node.js
node --version
npm --version

# Проверка Python
python3 --version
pip3 --version

# Проверка wscat
wscat --version
```

Все команды должны работать! ✅

## Быстрый тест

После пересборки запустите:

```bash
# Подключитесь к логам
wscat -c wss://vibee-mcp.fly.dev/ws/logs
```

Вы должны увидеть логи в реальном времени! 🎉

## Альтернативные способы

### Через меню

1. Кликните на зеленую кнопку в **левом нижнем углу** VS Code
2. В меню выберите `Rebuild Container`

### Через терминал

```bash
# Закройте VS Code
# Откройте снова
# Контейнер пересоберется автоматически
```

## Что делать, если не работает

### Проблема: Команды не найдены

**Решение**: Перезапустите терминал
```bash
exit
# Откройте новый терминал (Ctrl + `)
```

### Проблема: Пересборка зависла

**Решение**: 
1. Закройте VS Code
2. Удалите контейнер вручную:
   ```bash
   docker ps -a | grep vibee
   docker rm -f <container_id>
   ```
3. Откройте VS Code снова

### Проблема: Нет прав на установку

**Решение**: Проверьте, что вы пользователь `vscode`:
```bash
whoami
# Должно вывести: vscode
```

## После пересборки

Используйте новые инструменты:

```bash
# 1. Смотрите логи через wscat
wscat -c wss://vibee-mcp.fly.dev/ws/logs

# 2. Или через Python
python3 test_ws.py

# 3. Или через автоматический скрипт
./watch-logs.sh

# 4. Или откройте HTML в браузере
open test_ws.html
```

## Документация

- `DEVCONTAINER_SETUP.md` - Подробная настройка
- `LOGS_PROTOCOL.md` - Протокол WebSocket
- `LOGS_GUIDE.md` - Общее руководство

## Итог

**Пересоберите контейнер один раз** → Получите все инструменты → Смотрите логи! 🚀

---

**Нужна помощь?** Откройте issue или спросите в чате.
