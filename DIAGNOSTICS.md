# 🔍 Диагностика системы логирования

## ✅ Что работает:

1. **Polling**: Работает каждые 5 секунд
2. **Обработка чатов**: Обрабатывается 50 диалогов
3. **Логирование**: Сообщения логируются через `logging.quick_info()`
4. **log_aggregator**: Получает логи и транслирует
5. **WebSocket**: 3 подписчика подключены
6. **Broadcasting**: Логи отправляются подписчикам

## 📊 Статистика из логов:

```
[POLL] 🔍 Total dialogs found: 50
[POLL] 📋 Processing ALL chats for logging...
[LOG_AGG] Broadcasting log to 3 subscribers
```

## 📨 Примеры залогированных сообщений:

```
📨 TG: 144022504 User: бро!!
📨 TG: -5082217642 Ars Ariiskii: Красава
📨 TG: 5732975798 User: привет
📨 TG: 412973735 User: начинающие разработчики
```

## ⚠️ Проблема: Не все сообщения логируются

### Возможные причины:

1. **Сообщения пропускаются как SKIP (seen)**
   - Если сообщение уже было обработано, оно пропускается
   - Решение: Очистить кэш seen_ids

2. **Исходящие сообщения (OUT) не логируются**
   - Сообщения от бота помечаются как OUT и пропускаются
   - Это правильное поведение

3. **Polling не успевает обработать все чаты**
   - 50 чатов * 5 сообщений = 250 запросов
   - Может быть timeout или rate limit

4. **Сообщение пришло между циклами polling**
   - Polling каждые 5 секунд
   - Если сообщение пришло сразу после polling, нужно ждать 5 секунд

## 🔧 Решения:

### 1. Увеличить лимит сообщений на чат

Сейчас: `limit=5` в get_history
Изменить на: `limit=20`

### 2. Логировать ВСЕ сообщения (включая OUT)

Добавить флаг для логирования исходящих сообщений.

### 3. Добавить real-time WebHook

Вместо polling использовать WebHook от Telegram для мгновенного получения сообщений.

### 4. Увеличить частоту polling

Сейчас: каждые 5 секунд
Изменить на: каждые 2 секунды

## 📋 Текущая конфигурация:

- **Polling interval**: 5 секунд
- **Messages per chat**: 5
- **Total chats**: 50
- **Digital Twin**: OFF (обрабатываются ВСЕ чаты)
- **WebSocket subscribers**: 3
- **log_aggregator buffer**: 1000 логов

## 🎯 Рекомендации:

1. **Откройте dashboard и оставьте открытым**
   ```
   https://vibee-mcp.fly.dev/dashboard
   ```

2. **Подождите 5-10 секунд после отправки сообщения**
   - Polling работает каждые 5 секунд
   - Сообщение появится в следующем цикле

3. **Проверьте, что сообщение не OUT**
   - Если вы отправили сообщение от имени бота, оно будет помечено как OUT
   - OUT сообщения не логируются (это правильно)

4. **Обновите страницу (F5)**
   - WebSocket может отключиться
   - Обновление страницы переподключит

## 🔍 Как проверить конкретное сообщение:

1. Отправьте сообщение в Telegram
2. Подождите 10 секунд
3. Проверьте логи сервера:
   ```bash
   flyctl logs --app vibee-mcp | grep "текст_сообщения"
   ```

4. Если нашли `NEW INCOMING` - значит сообщение обработано
5. Если нашли `SKIP (seen)` - значит уже было обработано ранее
6. Если нашли `OUT` - это исходящее сообщение от бота

## ✅ Итог:

**Система логирования работает!** Все входящие (IN) сообщения логируются и транслируются через WebSocket.

Если конкретное сообщение не появилось:
- Проверьте, что оно IN (не от бота)
- Подождите 5-10 секунд (polling interval)
- Обновите dashboard (F5)
- Проверьте логи сервера
