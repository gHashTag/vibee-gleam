# 🔐 VIBEE Telegram Authentication Guide

## ⚠️ Критическая информация

Для подключения к Telegram API требуются **реальные учетные данные**, которые может предоставить только владелец аккаунтов.

---

## 📋 Что нужно для аутентификации

### 1. Telegram API Credentials

**Получите от https://my.telegram.org/apps:**

```bash
TELEGRAM_API_ID=<ваш_числовой_id>
TELEGRAM_API_HASH=<ваш_32-символьный_хеш>
```

**Пример:**
```bash
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=0123456789abcdef0123456789abcdef
```

### 2. Доступ к телефонам для получения SMS кодов

- **@neuro_sage:** +7 (993) 342-04-65
- **@vibee_agent:** +66 6-2401-4170

---

## 🚀 Процесс аутентификации

### Шаг 1: Установите переменные окружения

```bash
export TELEGRAM_API_ID=<ваш_api_id>
export TELEGRAM_API_HASH=<ваш_api_hash>
```

### Шаг 2: Запустите telegram-bridge

```bash
cd telegram-bridge
export PATH=$PATH:/usr/local/go/bin
go run ./cmd/server
```

Вы увидите:
```
🚀 Starting Telegram Bridge...
📊 Server listening on :8080
```

### Шаг 3: Аутентифицируйте первый аккаунт (@neuro_sage)

**3.1. Отправьте номер телефона:**

```bash
curl -X POST http://localhost:8080/api/v1/auth/phone \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "+79933420465"
  }'
```

**Ответ:**
```json
{
  "status": "code_sent",
  "phone_code_hash": "abc123..."
}
```

**3.2. Получите SMS код на телефон +7 (993) 342-04-65**

**3.3. Отправьте код:**

```bash
curl -X POST http://localhost:8080/api/v1/auth/code \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "+79933420465",
    "code": "12345",
    "phone_code_hash": "abc123..."
  }'
```

**Ответ при успехе:**
```json
{
  "status": "authorized",
  "session_id": "session_neuro_sage",
  "user": {
    "id": 123456789,
    "username": "neuro_sage",
    "phone": "+79933420465"
  }
}
```

**Если требуется 2FA пароль:**
```json
{
  "status": "password_required"
}
```

**Отправьте пароль:**
```bash
curl -X POST http://localhost:8080/api/v1/auth/password \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "+79933420465",
    "password": "ваш_2fa_пароль"
  }'
```

### Шаг 4: Аутентифицируйте второй аккаунт (@vibee_agent)

Повторите шаги 3.1-3.3 для номера **+66 6-2401-4170**

```bash
# Отправить номер
curl -X POST http://localhost:8080/api/v1/auth/phone \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "+6662401417"
  }'

# Получить SMS код на +66 6-2401-4170

# Отправить код
curl -X POST http://localhost:8080/api/v1/auth/code \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "+6662401417",
    "code": "12345",
    "phone_code_hash": "xyz789..."
  }'
```

### Шаг 5: Проверьте активные сессии

```bash
curl http://localhost:8080/api/v1/sessions
```

**Ответ:**
```json
{
  "sessions": [
    {
      "session_id": "session_neuro_sage",
      "phone": "+79933420465",
      "username": "neuro_sage",
      "authorized": true
    },
    {
      "session_id": "session_vibee_agent",
      "phone": "+6662401417",
      "username": "vibee_agent",
      "authorized": true
    }
  ]
}
```

---

## ✅ После успешной аутентификации

### Файлы сессий будут сохранены:

```
telegram-bridge/sessions/
├── session_neuro_sage.dat
└── session_vibee_agent.dat
```

### Настройте VIBEE для использования сессий:

```bash
cd gleam
export TELEGRAM_SESSION_ID=session_neuro_sage
export TELEGRAM_SESSION_1_PHONE="+79933420465"
export TELEGRAM_SESSION_1_USERNAME="neuro_sage"
export TELEGRAM_SESSION_2_PHONE="+6662401417"
export TELEGRAM_SESSION_2_USERNAME="vibee_agent"
export VIBEE_MODE=mcp

gleam run
```

---

## 🔧 Готовые скрипты для аутентификации

В папке `telegram-bridge/` есть готовые скрипты:

```bash
# Аутентификация @neuro_sage
./auth-neuro-sage.sh

# Проверка статуса
./check-status.sh

# Полный flow аутентификации
./test-flow.sh
```

---

## ⚠️ Важные замечания

1. **API credentials обязательны** - без них невозможно подключиться к Telegram
2. **SMS коды** - нужен доступ к телефонам для получения кодов
3. **2FA пароль** - если включена двухфакторная аутентификация
4. **Сессии сохраняются** - после первой аутентификации можно использовать повторно
5. **Безопасность** - храните API credentials и session файлы в секрете

---

## 🎯 Текущий статус

✅ **Система готова:**
- Erlang/OTP 27 установлен
- Go 1.21.5 установлен
- telegram-bridge скомпилирован
- Все RAG инструменты готовы
- Session manager настроен

⚠️ **Требуется от пользователя:**
- TELEGRAM_API_ID
- TELEGRAM_API_HASH
- Доступ к телефонам для SMS кодов
- 2FA пароли (если включены)

---

## 📞 Контакты для аутентификации

**Аккаунт 1 (@neuro_sage):**
- Телефон: +7 (993) 342-04-65
- Нужен доступ для получения SMS кода

**Аккаунт 2 (@vibee_agent):**
- Телефон: +66 6-2401-4170
- Нужен доступ для получения SMS кода

---

**Без реальных API credentials и доступа к телефонам аутентификация невозможна.**

Это требование безопасности Telegram API.

---

**Generated:** 2025-12-17 18:07 UTC  
**System:** VIBEE Agent Framework v0.1.0
